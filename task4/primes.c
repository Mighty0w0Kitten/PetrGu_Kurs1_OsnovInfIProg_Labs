#include <stdio.h>  /* Подключаем библиотеку для работы с вводом/выводом */

/* Основная функция программы */
int main() {
    /* В этой функции будет размещаться основная логика программы.
       Пока функция ничего не делает, просто возвращает 0, что означает успешное завершение программы. */
    
    return 0;  /* Завершаем выполнение программы с кодом 0 (успех) */
}

